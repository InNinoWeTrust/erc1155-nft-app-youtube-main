import { defineChain } from "thirdweb";

// Define the chain with chain ID or predefined chain name
export const chain = defineChain(137);

// If you want to use a custom chain, you can define it like this:  
